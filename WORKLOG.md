# 작업 로그 — Project A : 브랜드 아이덴티티 생성기

대전 Codyssey · AI 활용 실습 Term Project · 6팀 (4인 1팀)
GitHub: `A2-1`

---

## 1. 팀 구성 및 역할

| 역할 | 담당자 | 산출 파일 | 담당 기능 |
|---|---|---|---|
| ① 통합 · 입력 (팀장) | 주시호 | `main.py` `contracts.py` `fixtures.py` `brief.json` | CLI 대화형 입력, 브리프 검증, 단계 격리 실행, `brand_result.json` 조립, 전체 통합 |
| ② 네이밍 · 카피 | 신재풍 | `naming.py` | 브랜드명 후보 3~5개, 슬로건 3개 |
| ③ 스토리 · 컬러 | 이채원 | `content.py` | 브랜드 스토리, 컬러 팔레트 추천 |
| ④ 비주얼 | 김동호 | `visual.py` | 컬러 팔레트 시각화(matplotlib), AI 로고 시안 |

**협업 방식**
팀원이 각자 파일을 작성해 팀장에게 전달 → 팀장이 규격 검증 · 통합 테스트 후 GitHub에 커밋.
GitHub 커밋은 팀장 계정으로 단일화했으므로, 팀원별 기여 내역은 아래 4항(수령 · 통합 이력)과 5항(프롬프트 기록)으로 관리한다.

---

## 2. 사용 도구 및 환경

| 항목 | 내용 |
|---|---|
| Python | 3.10+ |
| 텍스트 LLM | Google Gemini (`gemini-3.1-flash-lite`) |
| 이미지 생성 | OpenAI `gpt-image-1` (강사 지급 키) |
| 주요 패키지 | `google-genai`, `openai`, `python-dotenv`, `matplotlib`, `requests` |
| 개발 환경 | Windows · VSCode · PowerShell |
| 키 관리 | `.env` (`.gitignore`로 제외), 제출용 견본은 `.env.example` |

각 팀원이 개발 보조로 사용한 LLM은 서로 달랐다(팀장·김동호 Claude, 신재풍·이채원 각자 사용 도구). 이로 인해 초기 구현 방향이 갈린 사례가 있었으며, 3-2항에 기록했다.

---

## 3. 주요 의사결정

### 3-1. 설계 단계 (STEP 1)

| 결정 사항 | 근거 | 제기자 |
|---|---|---|
| 브리프 컨셉을 "야간 근무자용 디카페인 음료"로 확정 | 업종을 좁게 잡아야 생성 결과가 구체적으로 나옴. 체크포인트 "결과가 너무 일반적이지 않고 구체적인가" 대응 | 팀 전체 |
| 파일을 `main` / `naming` / `content` / `visual` 4개로 분리 | 강의자료 협업 데이터 흐름도 구조를 따름. 파일이 겹치지 않아 통합 시 충돌이 없음 | 팀 전체 |
| **모든 생성 함수는 실패 시 예외 대신 `None` 반환** | API 하나가 실패해도 전체가 중단되지 않게 함. 과제 요구사항 "API 실패 시 다음 단계 계속 진행" 충족 | 팀장 |
| `brand_result.json` 조립은 `main.py`에서만 수행 | 각 모듈은 자기 반환값까지만 책임. 결과 형식이 한 곳에서 관리됨 | 팀장 |
| 파일 생성 함수는 저장 후 경로 문자열을 반환 | `visual.py`가 저장까지 맡고 `main.py`는 경로만 기록 | 팀장 |
| LLM 호출을 `contracts.call_llm()` 공용 헬퍼로 일원화 | 세 모듈이 같은 코드를 중복 구현하지 않도록. API 키 관리와 JSON 파싱을 한 곳에 집중 | 팀장 |
| `fixtures.py`(더미 데이터) 도입 | 컬러 팔레트가 있어야 로고를 만들 수 있어 비주얼 담당이 대기하게 됨. 목 데이터로 4명 병렬 개발 가능 | 팀장 |

### 3-2. 규격 협의 및 수정

| 결정 사항 | 근거 | 제기자 |
|---|---|---|
| `generate_slogans` / `generate_story` / `generate_logos`에 `brand_name` 인자 추가 | 브랜드명은 브리프에 없고 `generate_naming`의 산출물임. 이름을 모르면 슬로건·로고가 브랜드와 무관해짐 | 김동호 |
| 대표 브랜드명은 `naming[0]` 사용 | LLM에 추천 순서대로 정렬하도록 요청해 첫 항목을 대표안으로 삼음. 후보 순서에 의미를 부여 | 팀장 |
| 브랜드명 반환 구조를 `{"name_ko", "name_en", "meaning"}`로 확정 | 이미지 생성 모델이 한글을 거의 못 그려 로고 프롬프트에 영문 표기가 필요. 보너스 과제(다국어 네이밍) 대비 | 신재풍 · 팀장 |
| 컬러 팔레트를 `{"main": {"name","hex"}, "subs": [...]}` 구조로 변경 | 과제 예시 출력이 `#2E7D32 (Forest Green)` 형태로 컬러명을 함께 표기함 | 김동호 |
| 컬러명은 영문으로 제한 | matplotlib 한글 폰트 미설정 시 팔레트 PNG에서 글자가 깨짐 | 이채원 · 김동호 |
| 브랜드명 키를 `name_ko` 등으로 **유지**(재명명 반려) | 이미 `contracts.py`에 고정되어 있어 재명명의 실익이 없고 4개 파일 수정 비용만 발생 | 팀장 |
| `brand_result.json`에 `errors` 배열 기록 | 함수 반환 타입은 그대로 두고 `main.py`에서 실패 단계명만 수집. 실패 이력을 결과물에 남김 | 김동호 |
| 네이밍 실패 시에도 대체값으로 다음 단계 진행 | 뒤 단계를 통째로 건너뛰면 결과물이 지나치게 비게 됨. 팔레트·로고라도 생성되는 쪽을 택함 | 팀장 |

### 3-3. 기술 선택

| 결정 사항 | 근거 |
|---|---|
| 텍스트 LLM을 Gemini로 통일 | 무료 티어로 팀원 각자 키 발급이 가능. OpenAI 텍스트 키는 팀에 없음 |
| 이미지 API는 팀장이 키를 보관하고 비주얼 담당에게만 전달 | 강사 지급 키가 1개뿐이며 실제 비용이 발생. 4명이 동시 사용 시 한도 소진 위험 |
| `LOGO_COUNT` 상수로 로고 생성 수를 조절 | 프롬프트를 다듬는 동안 1장으로 줄여 호출 비용을 억제. 최종 확인 시에만 2~3장으로 상향 |
| 모델을 `gemini-3.1-flash-lite`로 고정 | 무료 티어 일일 한도가 넉넉함. `-preview`가 아닌 정식 버전이라 갑작스러운 단종 위험이 낮음 |

---

## 4. 파일 수령 · 통합 이력

| 담당 | 파일 | 단독 실행 | 통합 결과 | 조치 |
|---|---|---|---|---|
| 이채원 | `content.py` | 통과 | 스토리·컬러 정상 생성 | 규격 준수 확인 후 그대로 반영 |
| 김동호 | `visual.py` | 통과 | 팔레트 PNG · 로고 PNG 정상 저장 | 규격 준수 확인 후 그대로 반영 |
| 신재풍 | `naming.py` | — | **규격 불일치 2건 발견** | 팀장이 수정 후 반영 (아래 참조) |

### `naming.py` 수정 내역 (팀장 조치)

전달받은 버전은 팀 규격 배포 이전의 초기 설계를 기준으로 작성되어 있어 다음을 수정했다.

| 항목 | 수정 전 | 수정 후 |
|---|---|---|
| 슬로건 시그니처 | `generate_slogans(brief)` | `generate_slogans(brief, brand_name)` |
| LLM 호출 | OpenAI 직접 호출 (`OPENAI_API_KEY`) | `contracts.call_llm()` 사용 |
| JSON 파싱 | `json.loads()` 직접 호출 | `contracts.extract_json()` 사용 |
| 반환 키 | `{"name", "meaning"}` | `{"name_ko", "name_en", "meaning"}` |
| **프롬프트 하드코딩** | `"회복", "밤", "리듬"` / `차분하고 다정한`을 문자열로 고정 | 브리프에서 값을 읽어오도록 변경 |

마지막 항목이 가장 중요했다. 프롬프트에 첫 브리프의 값이 박혀 있어, 다른 브리프를 입력해도 같은 방향의 결과가 나오는 상태였다. 규칙 STEP 5의 "새로운 `brief.json`으로 처음부터 끝까지 실행" 검증이 무의미해지므로 수정했다.

담당자 로그에 기록된 설계 의도(반환 구조, 검증 로직, 정렬 지시 등)는 그대로 유지했다.

---

## 5. 프롬프트 기록

각 함수의 프롬프트 **전문은 해당 소스 파일에 그대로 남아 있다.** 여기에는 프롬프트에 반영한 조건과 설계 의도를 기록한다.

### `generate_naming` — 신재풍

**입력으로 전달한 브리프 항목**
업종 / 타겟 / 키워드 / 톤앤매너 / 경쟁사 / 추가 요청사항

**프롬프트에 명시한 조건**
- 브랜드명 후보 3~5개
- 키워드가 담은 브랜드 이미지 반영
- 톤앤매너에 어울리는 어감
- 기억하기 쉽고 발음하기 쉬울 것
- 경쟁사와 차별화
- 추가 요청사항 준수 (예: 3글자 이내, 영문 표기 용이)
- 한글 표기(`name_ko`)와 영문 표기(`name_en`) 모두 생성
- 이름의 의미와 작명 의도 설명
- **가장 추천하는 순서대로 정렬** — `main.py`가 첫 항목을 대표안으로 사용하므로 순서에 의미가 있음

**출력 형식 강제**
설명이나 마크다운 없이 지정한 JSON 형식으로만 응답하도록 명시.

**결과 검증**
`extract_json()` 파싱 후, `name_ko`와 `name_en`이 모두 있는 항목만 통과시킨다. 유효 항목이 하나도 없으면 `None`을 반환한다.

### `generate_slogans` — 신재풍

**프롬프트에 추가로 전달한 정보**
확정된 대표 브랜드명(한글명 / 영문명 / 이름의 의미)

**조건**
- 슬로건 3개
- 브리프의 톤앤매너를 문장 분위기에 반영
- 브랜드명의 의미와 이어지는 문구
- 타겟이 자신의 이야기로 느낄 수 있을 것
- 짧고 기억하기 쉬운 문장

브랜드명을 함께 전달한 이유는, 이름을 모른 채 작성한 슬로건은 어떤 브랜드에나 붙을 수 있는 일반적인 문구가 되기 때문이다.

### `generate_story` — 이채원

**프롬프트에 포함한 정보**
브리프 6개 항목 + 브랜드 한글명 / 영문명 / 이름의 의미

**조건**
- 한국어, 300자 내외
- 탄생 배경 / 철학 / 가치와 비전 포함
- 브랜드명과 의미를 자연스럽게 반영
- 브리프의 톤앤매너 유지
- 제목이나 인사말 없이 본문만 출력

**응답 처리**
`.strip()` 후 불필요한 따옴표를 제거한다. 200자 미만 또는 400자 초과면 경고를 출력하되 결과 자체는 반환한다. 글자 수 조건을 지나치게 엄격히 적용해 정상 결과까지 실패 처리하지 않기 위한 판단이다.

### `generate_palette` — 이채원

**조건**
- 메인 컬러 1개, 서브 컬러 2~3개
- 각 컬러에 `name`(영문)과 `hex` 포함
- HEX는 `#RRGGBB` 6자리 형식
- 업종과 타겟, 브랜드 분위기 고려

**결과 검증**
최상위가 `dict`인지 → `main`·`subs` 키 존재 → 각 컬러의 `name`·`hex` 존재 → 컬러 개수(메인 1, 서브 2~3) → HEX 정규식 `^#[0-9A-Fa-f]{6}$` 순으로 검사한다.
이 검증으로 `green`, `#FFF`, `rgb(255,255,255)` 같은 형식 오류를 걸러낸다.

`generate_palette`는 브랜드명을 받지 않는다. 네이밍과 독립적으로 실행할 수 있게 하기 위한 구조다.

### `generate_logos` (이미지 프롬프트) — 김동호

**프롬프트 조립 재료 4가지**
1. `brand_name["name_en"]` — 이미지 모델이 한글을 거의 못 그리므로 영문 표기를 사용
2. `brand_name["meaning"]` — 시각적 컨셉의 힌트
3. `palette["main"]`의 컬러명과 HEX — 컬러명을 함께 넣으면 색 재현이 더 정확함
4. `brief["keywords"]`, `brief["tone"]`

**시안별 컨셉 분리**
세 장을 서로 다른 방향으로 생성한다.
- 심볼형 — 문자 없이 추상 심볼만
- 워드마크형 — 영문 브랜드명을 산세리프로
- 결합형 — 심볼을 브랜드명 위에 배치

**스타일 지시어**
`minimal vector logo`, `flat design`, `plain white background`, `no photograph`, `no gradient mesh`, `no extra text`
사진처럼 생성되는 것을 막고 로고다운 결과를 얻기 위한 지시다.

**응답 처리**
이미지 API 응답이 base64인지 URL인지에 따라 분기한다. base64면 디코딩하고, URL이면 `requests`로 다운로드(`timeout=60`)한 뒤 바이너리 모드(`"wb"`)로 저장한다.

**에러 처리**
장별로 `try/except`를 감싸 한 장이 실패해도 나머지는 저장하고 리스트로 반환한다. 전부 실패했을 때만 `None`을 반환한다.

---

## 6. 오류 및 해결 기록

| # | 발생 위치 | 증상 | 원인 | 해결 |
|---|---|---|---|---|
| 1 | 모듈 단독 실행 | `GEMINI_API_KEY가 없습니다` | `load_dotenv()`가 `main.py`에만 있어 `python content.py` 실행 시 `.env`를 읽지 않음 | `contracts.py` 최상단으로 이동. 이 파일을 import하는 모든 모듈이 `.env`를 읽게 됨 |
| 2 | `call_llm()` | `cannot import name 'genai' from 'google'` | `google-genai` 패키지 미설치 | `pip install -r requirements.txt` |
| 3 | `load_brief()` | `PermissionError: [Errno 13]` | 브리프 경로에 폴더(`.`)를 입력해 폴더를 파일처럼 열려 함 | `os.path.isdir()` 검사와 `JSONDecodeError` 처리를 추가해 안내 메시지 후 정상 종료 |
| 4 | `naming.py` | `generate_slogans() takes 1 positional argument but 2 were given` | 규격 배포 이전 버전으로 구현되어 시그니처가 불일치 | 4항의 수정 내역대로 통합 수정 |
| 5 | `naming.py` 프롬프트 | 다른 브리프를 넣어도 같은 방향의 결과가 나옴 | 브리프 값이 프롬프트에 문자열로 하드코딩됨 | 브리프에서 값을 읽어오도록 변경. `brief_test.json`으로 검증 |
| 6 | `call_llm()` | `429 RESOURCE_EXHAUSTED` (`limit: 20`) | 사용 모델의 무료 티어 일일 요청 한도(20회) 소진 | 일일 한도가 넉넉한 모델로 변경. 이후 흐름 확인은 `--mock` 모드로 대체해 호출 수 절감 |
| 7 | `call_llm()` | `404 NOT_FOUND ... no longer available to new users` | 지정한 모델이 신규 사용자에게 더 이상 제공되지 않음 | `client.models.list()`로 사용 가능 모델을 직접 조회한 뒤 `gemini-3.1-flash-lite`로 확정 |

### 에러 처리 설계가 실제로 검증된 사례

**6번(429) 발생 시** — 네이밍·슬로건·스토리 3단계가 정상 완료된 뒤 컬러 팔레트 단계에서 한도 초과가 발생했다. 프로그램은 중단되지 않았고, 로고 단계는 "컬러 팔레트 없음"으로 건너뛴 뒤 `brand_result.json`이 정상 저장되었다.

**7번(404) 발생 시** — 5개 단계가 **전부** 실패했음에도 프로그램이 종료되지 않고 `errors` 배열 5건과 함께 결과 파일을 생성했다.

두 사례 모두 의도적으로 만든 상황이 아니라 실제 개발 중 발생한 것으로, 과제 요구사항 "API 호출 실패 시 에러 메시지를 출력하고 다음 단계를 계속 진행한다"가 실동작으로 검증된 결과다.

김동호 담당자는 이와 별개로 `.env`의 이미지 API 키를 의도적으로 훼손해 401 오류를 유발하는 방식으로 장별 `try/except` 동작을 사전 검증했다.

---

## 7. 전체 파이프라인 검증 (규칙 STEP 5)

브리프를 교체해도 결과가 브리프를 따라 달라지는지 확인하기 위해, 성격이 상반된 두 번째 브리프로 전체 실행을 수행했다.

| | `brief.json` | `brief_test.json` |
|---|---|---|
| 업종 | 야간 근무자용 디카페인 음료 | 제주 로컬 수제 맥주 브루어리 |
| 키워드 | 회복 · 밤 · 리듬 | 바다 · 여유 · 발효 |
| 톤앤매너 | 차분하고 다정한 | 자유롭고 유쾌한 |
| 생성된 브랜드명 | 온율 · 안밤 · 토닥 · 결 | 서피 · 숨비 · 보글 · 까망 |
| 생성된 컬러 메인 | Midnight Indigo 계열 | (실행 결과 참조) |

제주 브리프에서는 해녀의 숨비소리, 현무암, 파도 등 브리프에만 존재하는 소재가 반영되었다. 6항 5번의 하드코딩 문제가 해소되었음을 확인한 검증이다.

---

## 8. 최종 산출물 점검

**제출 전 팀 전체가 함께 확인**

- [ ] `output/`에 `brand_result.json`, `color_palette.png`, `logo_01~02.png`가 모두 존재
- [ ] `brief.json`이 팀이 합의한 브랜드 아이디어를 반영
- [ ] `brief_test.json`으로 처음부터 끝까지 실행 성공 (7항)
- [ ] `LOGO_COUNT`가 2 이상으로 설정되어 있는지 확인
- [ ] 브랜드 스토리 글자 수가 300자 내외 범위인지 확인
- [ ] API 키가 결과물 · 코드 · 커밋 히스토리에 섞이지 않음 (`.env`가 리포에 없는지 확인)
- [ ] `.env.example`의 모델명이 실제 사용 모델과 일치
- [ ] 4명 모두 전체 파이프라인 흐름을 설명할 수 있음
- [ ] 생성된 브랜드명의 상표 중복 여부는 별도 확인이 필요함을 인지

**보너스 과제**
- [x] 한글 · 영문 네이밍 동시 생성 (`name_ko` / `name_en`)
- [ ] 경쟁사 분석 기반 차별화 포인트 제안

---

## 9. 작업을 통해 확인한 점

**LLM의 비정형 응답을 프로그램이 쓸 수 있는 형태로 만드는 과정이 실제 작업의 상당 부분을 차지했다.** 프롬프트를 잘 쓰는 것만으로는 부족했고, 코드펜스 제거, JSON 구조 검증, HEX 형식 정규식 검사 같은 방어 로직이 필요했다. 특히 컬러 팔레트는 `rgb()` 표기나 3자리 축약형이 섞여 들어올 수 있어 검증 없이는 이미지 생성 단계가 깨진다.

**"입력 → 처리 → 출력 규격을 먼저 맞춘다"는 규칙의 효과를 체감했다.** 파일을 나누기만 하고 규격을 정하지 않았다면 통합 시점에 네 명의 코드를 모두 고쳐야 했을 것이다. 실제로 규격이 어긋난 한 건(`naming.py`)에서 `TypeError`가 발생했고, 이는 규격이 없었다면 모든 파일에서 일어났을 문제였다.

**의존 관계가 있는 작업을 병렬화하는 방법을 배웠다.** 로고 생성은 컬러 팔레트를, 슬로건은 브랜드명을 필요로 한다. 더미 데이터(`fixtures.py`)를 먼저 만들어두었기에 네 명이 서로를 기다리지 않고 동시에 개발할 수 있었다.

**API는 개발 중에 조건이 바뀔 수 있다는 점을 확인했다.** 모델 단종(404)과 무료 한도 소진(429)을 실제로 겪었고, 두 경우 모두 코드가 아니라 외부 환경의 변화였다. 사용 가능한 모델을 코드로 조회해 확인하는 방법과, 개발 중 API 호출 수를 줄이는 목(mock) 모드의 필요성을 이해했다.

**에러 처리를 "실패해도 계속 진행"으로 설계한 것이 실제로 유효했다.** 개발 과정에서 예기치 않은 실패가 여러 번 발생했지만 프로그램이 한 번도 중단되지 않았고, 매번 어디까지 진행되었는지 확인할 수 있었다. 이는 제출 요건 충족을 넘어 개발 자체를 수월하게 만든 선택이었다.
