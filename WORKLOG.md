# 작업 로그 — Project A : 브랜드 아이덴티티 생성기

> 제출 체크리스트 항목: "작업 로그(프롬프트, 수정 이력)가 기록되어 있는가",
> "최종 보고서에 파트별 담당자와 주요 의사결정이 정리되어 있는가"
>
> 이 문서가 그대로 최종 보고서의 재료가 됩니다. 몰아서 쓰지 말고 그때그때 채우세요.

---

## 1. 팀 구성 및 역할

| 역할 | 담당자 | 산출 파일 | 담당 기능 |
|---|---|---|---|
| ① 통합 · 입력 (팀장) | 주시호 | `main.py` `contracts.py` `fixtures.py` `brief.json` | CLI, 브리프 검증, 전체 실행 구조, 결과 저장 |
| ② 네이밍 · 카피 | 신재풍 | `naming.py` | 브랜드명 3~5개, 슬로건 3개 |
| ③ 스토리 · 컬러 | 이채원 | `content.py` | 브랜드 스토리, 컬러 팔레트 추천 |
| ④ 비주얼 | 김동호 | `visual.py` | 팔레트 시각화, AI 로고 시안 |

**협업 방식:** 팀원이 각자 파일을 작성해 팀장에게 전달 → 팀장이 통합·테스트 후 GitHub에 커밋.
GitHub 리포: `A2-1`

---

## 2. 주요 의사결정

| 날짜 | 결정 사항 | 근거 | 제기자 |
|---|---|---|---|
| 2026-08-22 | 브리프 컨셉을 "야간 근무자용 디카페인 음료"로 확정 | 업종을 좁게 잡아야 생성 결과가 구체적으로 나옴 (체크포인트 "결과가 너무 일반적이지 않고 구체적인가") | 팀 전체 |
| 2026-08-22 | 모든 생성 함수는 실패 시 예외 대신 `None` 반환 | API 하나가 실패해도 전체가 죽지 않게 함. 과제 요구사항 "API 실패 시 다음 단계 계속 진행" 충족 | 팀장 |
| 2026-08-22 | `generate_logos` / `generate_slogans` / `generate_story`에 `brand_name` 인자 추가 | 브랜드명은 브리프에 없고 `generate_naming`의 산출물임. 이름을 모르면 로고·슬로건이 브랜드와 무관해짐 | (팀원명) |
| 2026-08-22 | 대표 브랜드명은 `naming[0]` 사용 | LLM에 추천 순서대로 정렬하도록 요청 → 첫 항목이 대표안 | 팀장 |
| 2026-08-22 | 컬러 팔레트에 컬러명(`name`) 추가, `{"hex", "name"}` 구조로 변경 | 과제 예시 출력에 `#2E7D32 (Forest Green)` 형태로 컬러명이 표기됨 | (팀원명) |
| 2026-08-22 | 브랜드명 키는 `name_ko`/`name_en`/`meaning` 유지 | 이미 `contracts.py`에 고정되어 있어 재명명의 실익이 없고 4개 파일 수정 비용만 발생 | 팀장 |
| 2026-08-22 | `brand_result.json`에 `errors` 배열 기록 | 함수 반환 타입은 그대로 두고 `main.py`에서 실패 단계명만 수집 | (팀원명) |
| 2026-08-22 | `fixtures.py`(더미 데이터) 도입 | 컬러 팔레트가 있어야 로고를 만들 수 있어 비주얼 담당이 대기하게 됨. 목 데이터로 4명 병렬 개발 | 팀장 |

---

## 3. 일자별 진행

### 2026-08-22 (강의 · 킥오프)

| 담당 | 한 일 | 결과 |
|---|---|---|
| 팀장 | 브리프 확정, 함수 입출력 규격 합의, 스켈레톤 배포, GitHub 리포 생성 | STEP 1 완료 |
| ② | | |
| ③ | | |
| ④ | | |

### (날짜)

| 담당 | 한 일 | 결과 |
|---|---|---|
| | | |

---

## 4. 파일 수령 · 통합 이력

> 팀원 커밋이 GitHub에 직접 남지 않는 방식이므로, 기여 내역을 여기에 기록합니다.

| 날짜 | 담당 | 받은 파일 | 단독 실행 | `--mock` 통합 | 커밋 | 비고 |
|---|---|---|---|---|---|---|
| | | | | | | |

---

## 5. 프롬프트 기록

> 각 담당자가 실제로 사용한 프롬프트를 남깁니다. 여러 번 고쳤다면 최종본과 함께 무엇을 왜 바꿨는지 적으세요.

### `generate_naming` — 담당: (이름)

```
(프롬프트 전문)
```

**수정 이력**
- v1 → v2: (무엇이 문제였고 어떻게 바꿨는지)

### `generate_slogans` — 담당: (이름)

```
(프롬프트 전문)
```

### `generate_story` — 담당: (이름)

```
(프롬프트 전문)
```

### `generate_palette` — 담당: (이름)

```
(프롬프트 전문)
```

### `generate_logos` (이미지 프롬프트) — 담당: (이름)

```
(프롬프트 전문)
```

---

## 6. 오류 및 해결 기록

> 체크포인트 "에러 처리 상황 점검" 항목의 근거 자료가 됩니다.

| 발생 위치 | 증상 | 원인 | 해결 방법 |
|---|---|---|---|
| | | | |

**미리 예상되는 항목 (겪으면 위 표에 옮겨 적을 것)**
- LLM 응답이 코드펜스(```)에 감싸여 와서 `json.loads` 실패 → `contracts.extract_json()`으로 처리
- 컬러 팔레트 PNG에서 한글 라벨이 □□□로 깨짐 → 라벨을 영문 컬러명 + HEX로 표기
- 이미지 API 호출 실패 시 로고 3장 중 일부만 생성 → 장별 `try/except`로 부분 성공 허용

---

## 7. 최종 산출물 점검

**제출 전 팀 전체가 함께 확인**

- [ ] `output/`에 `brand_result.json`, `color_palette.png`, `logo_01~02.png`가 모두 존재
- [ ] `brief.json`이 팀이 합의한 브랜드 아이디어를 반영
- [ ] `brief_test.json`(제주 수제 맥주)으로 처음부터 끝까지 실행 성공 — 결과가 첫 브리프와 확연히 다른지 확인
- [ ] API 키가 결과물·코드·커밋 히스토리에 섞이지 않음 (`.env`가 리포에 없는지 확인)
- [ ] 4명 모두 전체 파이프라인 흐름을 설명할 수 있음
- [ ] 이 작업 로그의 빈칸이 모두 채워짐

**보너스 과제 (선택)**
- [ ] 경쟁사 분석 기반 차별화 포인트 제안
- [ ] 한글 · 영문 네이밍 동시 생성 (`name_en` 필드로 이미 구조는 준비됨)
